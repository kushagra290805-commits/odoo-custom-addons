const { JSDOM } = require('jsdom'); const dom = new JSDOM('<!DOCTYPE html><canvas id="canvas3d"></canvas>'); global.window = dom.window; global.document = dom.window.document; global.navigator = dom.window.navigator; global.Image = dom.window.Image; global.HTMLCanvasElement = dom.window.HTMLCanvasElement; const { Application } = require('@splinetool/runtime'); const canvas = document.getElementById('canvas3d'); canvas.getContext = () => null; const app = new Application(canvas); console.log(JSON.stringify({status: 'success', initialized: true}));
